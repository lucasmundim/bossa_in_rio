<%= form_for @photo, :url => admin_page_photo_path(@page, @photo), :html => {:multipart => true, :class => "form-stacked"} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>