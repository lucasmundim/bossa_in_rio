<%= form_for @photo, :url => imageable_photo_path(@imageable, @photo), :html => {:multipart => true, :class => "form-stacked"} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>